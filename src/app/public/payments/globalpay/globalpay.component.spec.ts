/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GlobalpayComponent } from './globalpay.component';

describe('GlobalpayComponent', () => {
  let component: GlobalpayComponent;
  let fixture: ComponentFixture<GlobalpayComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GlobalpayComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(GlobalpayComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
