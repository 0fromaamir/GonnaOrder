/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { RealexpaymentComponent } from './realexpayment.component';

describe('RealexpaymentComponent', () => {
  let component: RealexpaymentComponent;
  let fixture: ComponentFixture<RealexpaymentComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ RealexpaymentComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(RealexpaymentComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
